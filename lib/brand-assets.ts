/** Default raster lockup (may include baked-in background — use with dark-surface blend). */
export const LOGO_RASTER_PATH = "/logo.png";

/** Crisp transparent wordmark for dark chrome (nav / footer). */
export const LOGO_WORDMARK_LIGHT_SVG = "/brand/pbpp-wordmark-light.svg";

export const LOGO_INTRINSIC = { width: 1024, height: 682 } as const;
