# Public site
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Admin API (server only — never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=

# PBPP Operations login (plain password; set a strong value in production)
ADMIN_PASSWORD=
# Salt for session cookie derivation (any random string)
ADMIN_COOKIE_SECRET=
